# Devlog：<batch 名>

对应 plan：<路径>。每个增量追加一节，不修改历史节。

---

## 增量 <n>：<一句话说明>

```text
commits:  <BASE..HEAD 或 SHA 列表>
worktree: <路径>
```

### 变更说明

<做了什么、关键技术选择及理由（可逆技术选择不必事前请示，但要留痕）>

### 证据

```text
cmd:    <原样可复制的命令>
scope:  <覆盖 diff 中哪些文件/路径>
result: <pass/fail + 关键行>
noise:  <已知与本 diff 无关的失败>
```

### 已知限制 / 未验证面

### 被暂停的 slice（如有）

<已知事实 / 可选方案及影响 / 推荐选择 / 看板卡链接>
