# Outcome Roadmap

> Goal 多到不能靠一个短列表管理时才使用。

| 顺序 | Goal | Outcome | Status | Owner | Blocked by |
|---:|---|---|---|---|---|

状态只用 `QUEUED / ACTIVE / BLOCKED / DONE / CANCELLED`。这里负责排序和依赖；Goal 的范围、证据与结果仍写在
各自 Goal 文件里。
