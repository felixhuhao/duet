# 跨界消息：<一行摘要>

落点：发送方仓 `docs/duet/outbox/YYYY-MM-DD-<type>-<slug>.md`，commit 后建
inbox 卡（协议见 duet protocol/inbox.md）。

```text
from:   <轨/仓 + 发出时 commit>
to:     <目标 repo / worktree>
type:   移交单 | 需求 | 契约变更通知 | FYI
到期点: <处理时限；无则写「下次 closure 顺带」>
```

## 事实

<发生了什么，带代码/文档锚。对没有发送方上下文的读者自含>

## 请求

<要接收方做什么、怎样算做完；FYI 类可空>

## 回执

<接收方登账链接；关卡动作即送达确认>
