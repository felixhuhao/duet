# templates

新工作默认只复制 [goal.md](goal.md)。项目已有等价格式时继续用原格式，不为 duet 另建目录。

Goal 多到需要统一排序时，才使用 `outcome-roadmap.md`；多个 Goal 的昂贵集成/真机验证需要集中跟踪时，
才使用 `validation-ledger.md`。

`plan.md`、`plan-review.md`、`review.md`、`devlog.md`、`message.md` 和 `frontmatter.md` 仅供旧 batch
兼容，不用于新的 solo Goal。
